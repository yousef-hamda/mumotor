# Base URL for the API. In dev, Vite proxies /api to the backend (see vite.config.ts),
# so the default works out of the box.
VITE_API_URL=/api

# "Continue with Google" button appears only when this is set (same value as backend GOOGLE_CLIENT_ID).
VITE_GOOGLE_CLIENT_ID=""
