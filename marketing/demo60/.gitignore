# Heavy build artifacts — regenerate via capture_lang.mjs + render_lang.mjs
shots/
shots_*/
frames_*/
audio/
*.mp4
*.png
*.log
node_modules/

# One-shot scratch siblings of the tracked *_lang.* pipeline (not reproducible,
# mutate the local demo DB) — kept locally, never committed.
capture_account.mjs
daily_report.html
deco_check.mjs
film.html
music.py
